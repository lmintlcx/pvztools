{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "${workspaceRoot}/scripts/build_debug.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}